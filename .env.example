# Service Ports
WEB_PORT=
API_PORT=
AI_PORT=
POSTGRES_PORT=

# Caddy Reverse Proxy Ports
CADDY_HTTP_PORT=
CADDY_HTTPS_PORT=

# PostgreSQL Database
POSTGRES_DB=
POSTGRES_USER=
POSTGRES_PASSWORD=

# Internal Service Connections
DATABASE_URL=
AI_SERVICE_URL=

# CORS Configuration
CORS_ALLOWED_ORIGINS=

# Frontend Configuration
VITE_API_BASE_URL=

# Large Language Model (LLM)
# Keep disabled by default. The mock provider is for local tests only; no real provider is enabled yet.
LLM_PROVIDER=disabled
LLM_API_KEY=

# JWT
JWT_SECRET=

# Social sign-in (leave client IDs and secrets empty to disable a provider)
OAUTH_WEB_REDIRECT_URL=
GOOGLE_OAUTH_CLIENT_ID=
GOOGLE_OAUTH_CLIENT_SECRET=
GOOGLE_OAUTH_REDIRECT_URL=
