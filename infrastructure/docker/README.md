# Container infrastructure

Runtime Dockerfiles remain beside each application so ownership is clear. Shared future container support files can be added here. Compose uses a private application network, named volumes, health-gated startup, explicit configurable host ports, and modest memory limits.
