# API integration tests

Cross-package and future database-backed integration tests belong here. Current HTTP behavior is tested beside the handler with dependency interfaces and deterministic stubs.
