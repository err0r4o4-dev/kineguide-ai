# Shared UI

KineGuide AI currently has one frontend, so application components remain in `apps/web`. Move a component here only after it is reused by multiple applications, its accessibility contract is tested, and its API is independent of an application feature.

This package is private and is not configured for publication. The current shared asset is design-token guidance, not a speculative component framework.
